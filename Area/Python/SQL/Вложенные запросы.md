# 1
Вложенный запрос (Подзапрос | Внутренний запрос)
# Возврат 1 значения

Запрос может быть включен в часть другого запроса ??Любого напр. WHERE
``` SQL
SELECT ...
FROM ...
WHERE tank = (SELECT ...)
```