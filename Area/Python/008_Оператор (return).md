# Общие сведения
По умолчанию функция возвращает None.
Возвращает значение из функции

return <ЛЮБОЙ ОБЪЕКТ | ВЫРАЖЕНИЕ>
``` python
def emx(par1):
	return par1
	return 1 + 1 --> Страбатывает только 1 return
```

