# Общие сведения
Используется для получения переменной из локального пространства имён уровнем выше.

	1. Нельзя так сослаться на переменную уже в глобальном пространстве имён.

``` python
X = 100

def exm(x):
    nonlocal X
    X += x

exm(1)
print(X) --> 101
```
