# Общие сведения
Используется для изменения глобальной переменной в локальном пространстве имён

	1. Если переменная изначально не была создана то global создаст её

``` python
X = 100

def exm(x):
    global X
    X += x

exm(1)
print(X) --> 101
```
